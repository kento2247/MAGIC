[project]
name = "mini-hackathon-2026"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "==3.10.0"
dependencies = [
    "diffusers",
    "python-pptx>=1.0.2",
    "transformers>=5.3.0",
]

[tool.uv.sources]
diffusers = { git = "https://github.com/huggingface/diffusers" }
